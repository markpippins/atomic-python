version: '3.8'

services:
  python-broker-service:
    build: .
    ports:
      - "8400:8400"
    volumes:
      - ./app:/app/app
    environment:
      - PYTHONPATH=/app
    restart: unless-stopped
    networks:
      - atomic-network

networks:
  atomic-network:
    driver: bridge
