"""timer is used to schedule blocks in which modes are either valid or not or to vary the behavior of rules"""

# set_param_timer(mode, param, value, interval)
# set_mode_param(mode, param, value, interval, timer)